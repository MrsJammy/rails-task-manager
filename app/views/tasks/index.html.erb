<h1>Tasks</h1>

<ul>
 <% @tasks.each do |task| %>
  <li><%= link_to task.content, task_path(task) %>
    <%= link_to 'edit', edit_task_path(task) %>
    <%= link_to 'delete', task_path(task), method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
  <% end %>
  <%= link_to 'create a new task', new_task_path %>
</ul>
